<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userxml">

<select id="sql_find" resultMap="mapxml.userMap" parameterType="map" statementType="CALLABLE">
	EXEC BUSCA_USUARIO  #{tipo},#{login},#{pwd}
</select>

</mapper>

